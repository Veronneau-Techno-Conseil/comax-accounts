@using CommunAxiom.Accounts.Contracts
@model EcosystemVersionTag
@inject ILookupStore lookupStore
@{
    ViewData["Title"] = "Application Version Tag Details";
}

<div class="card w-25" style="margin-bottom: 1em;">
    <div class="card-header">
        <div class="card-header-title">
            <p class="title is-4">
                @ViewData["Title"]
            </p>
        </div>
        <div class="card-header-icon">
            <a asp-controller="ApplicationType" asp-action="Details" asp-route-ecosys="@Model.EcosystemVersion.EcosystemId" asp-route-id="@Model.EcosystemVersionId"><img src="~/Images/Back.png" class="icon" /></a>
        </div>
    </div>
    <div class="card-content">
        <div class="field">
            <label asp-for="EcosystemVersion" class="label"></label>
            <span class="is-primary">@Model.EcosystemVersion.Ecosystem.Name</span>
        </div>
        <div class="field">
            <label asp-for="EcosystemVersion.VersionName" class="label"></label>
            <span class="is-primary">@Model.EcosystemVersion.VersionName</span>
        </div>
        <div class="field">
            <label asp-for="AppVersionTag.ApplicationType" class="label"></label>
            <span class="is-primary">@Model.AppVersionTag.ApplicationType.Name</span>
        </div>
        <div class="field">
            <label asp-for="AppVersionTag" class="label"></label>
            <span class="is-primary">@Model.AppVersionTag.Name</span>
        </div>
    </div>
</div>


<div class="card w-25">
    <div class="card-header">
        <div class="card-header-title">
            <p class="title is-4">
                Application Version Configurations
            </p>
        </div>
    </div>
    <div class="card-content">
        <table class="table">
            <thead>
                <tr>
                    <th><abbr title="Key">Configuration Key</abbr></th>
                    <th><abbr title="Default">DefaultValue</abbr></th>
                    <th><abbr title="Generator">Value Generator</abbr></th>
                    <!--/management/appclaim/{appNs}/create-->
                    <th><abbr title=""><a asp-controller="AppVersionConfig" asp-area="Management" asp-action="Add" asp-route-id="@Model.EcosystemVersionId" asp-route-ecosys="@Model.EcosystemVersion.EcosystemId"><img class="icon" src="~/Images/Add.png" /></a></abbr></th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model.EcosystemConfigs)
                {
                    <tr>
                        <td>@item.ConfigurationKey</td>
                        <td>@item.DefaultValue</td>
                        <td>@item.ValueGenerator</td>
                        <td>
                            @*<a asp-action="Details" asp-area="Management" asp-controller="AppVersionConfig" asp-route-tagid="@item.AppVersionTagId" asp-route-apptype="@Model.ApplicationTypeId" asp-route-id="@item.Id"><img src="~/Images/View.png" class="icon" /> </a>
                            <a asp-action="Edit" asp-area="Management" asp-controller="AppVersionConfig" asp-route-tagid="@item.AppVersionTagId" asp-route-apptype="@Model.ApplicationTypeId" asp-route-id="@item.Id"><img src="~/Images/Edit.png" class="icon" /> </a>
                            <a asp-action="Remove" asp-area="Management" asp-controller="AppVersionConfig" asp-route-tagid="@item.AppVersionTagId" asp-route-apptype="@Model.ApplicationTypeId" asp-route-id="@item.Id"><img src="~/Images/Delete.png" class="icon" /> </a>*@

                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>